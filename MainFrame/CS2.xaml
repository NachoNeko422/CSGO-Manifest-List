<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App1.MainFrame.CS2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1.MainFrame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <ItemsControl x:Name="ManifestList" Margin="10">
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="local:ManifestEntry">
                    <StackPanel Orientation="Horizontal" Spacing="10" Margin="2">
                        <TextBox Text="{x:Bind ManifestId}" 
                                 IsReadOnly="True" 
                                 BorderThickness="0" 
                                 Background="Transparent" 
                                 Width="300"
                                 FontSize="18"/>
                        <TextBlock Text="{x:Bind Date}" VerticalAlignment="Center" FontSize="16"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Page>
